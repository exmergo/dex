version: "0.2.0.dev0"
semantic_model:
- name: shop
  datasets:
  - name: orders
    source: demo.main.orders
    fields:
    - name: order_id
      expression: {dialects: [{dialect: ANSI_SQL, expression: order_id}]}
    primary_key: [order_id]
