"""The bigquery adapter (stub). Not yet implemented: metadata model, SQLGlot
dialect, cheap-metadata path, and the connector-specific cost strategy
(bytes scanned).
"""

from __future__ import annotations

# Cost paradigm for this connector: bytes scanned.
PARADIGM = "bytes_scanned"
DIALECT = "bigquery"
